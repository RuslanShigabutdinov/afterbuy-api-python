meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "login":"admin",
    "password":"root"
  }
}

settings {
  encodeUrl: true
}
